# OpenAPI Specification Generator - Environment Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# REQUIRED
# =============================================================================

# Anthropic API Key (Required)
# Get your key at: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-your-key-here

# =============================================================================
# OPTIONAL - LLM Configuration
# =============================================================================

# Claude model to use (default: claude-sonnet-4-20250514)
# Options: claude-sonnet-4-20250514, claude-3-5-sonnet-20241022, claude-opus-4-20250514
ANTHROPIC_MODEL=claude-sonnet-4-20250514

# LLM API call timeout in seconds (default: 120)
LLM_TIMEOUT=120

# =============================================================================
# OPTIONAL - Performance Tuning
# =============================================================================

# Maximum concurrent LLM API calls (default: 3)
# Higher = faster but more API load. Recommended: 3-5
MAX_CONCURRENT_LLM_CALLS=3

# Maximum concurrent HTTP requests (default: 5)
MAX_CONCURRENT_REQUESTS=5

# Delay between requests in seconds (default: 1.0)
# Increase for rate-limited sites
RATE_LIMIT_DELAY=1.0

# HTTP request timeout in seconds (default: 60)
REQUEST_TIMEOUT=60

# =============================================================================
# OPTIONAL - Caching Configuration
# =============================================================================

# Enable HTTP response caching (default: true)
# Set to false to disable caching of HTTP responses
ENABLE_HTTP_CACHE=true

# Enable LLM result caching (default: true)
# Set to false to always re-extract with LLM (costly!)
ENABLE_LLM_CACHE=true

# Cache directory (default: .cache)
CACHE_DIR=.cache

# Cache time-to-live in seconds (default: 86400 = 24 hours)
CACHE_TTL=86400

# =============================================================================
# OPTIONAL - Crawling Configuration
# =============================================================================

# Maximum pages to crawl per site (default: 50)
# Increase for larger documentation sites
MAX_PAGES_PER_SITE=50

# Maximum crawl depth (default: 3)
# How deep to follow links from the base URL
MAX_DEPTH=3

# =============================================================================
# OPTIONAL - Documentation Discovery Override (NEW!)
# =============================================================================

# Force specific documentation URLs (bypasses auto-discovery)
# Comma-separated list of URLs
# Use this when auto-discovery fails or for faster processing
# Example: FORCE_DOC_URLS=https://example.com/guide/,https://example.com/api/
# FORCE_DOC_URLS=

# =============================================================================
# OPTIONAL - Output Configuration
# =============================================================================

# Output directory for generated specs (default: output)
OUTPUT_DIR=output

# Logging level (default: INFO)
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# =============================================================================
# OPTIONAL - HTTP Configuration
# =============================================================================

# User agent for HTTP requests
USER_AGENT=OpenAPI-Generator-Bot/1.0 (Educational Project)

# =============================================================================
# NOTES
# =============================================================================
#
# 1. Quick Start:
#    - Copy this file: cp .env.example .env
#    - Add your API key to ANTHROPIC_API_KEY
#    - Run: openapi-gen https://api.example.com
#
# 2. Performance Tips:
#    - Increase MAX_CONCURRENT_LLM_CALLS for faster processing (costs more)
#    - Keep caching enabled for development (saves money on re-runs)
#    - Adjust RATE_LIMIT_DELAY for slow/rate-limited sites
#
# 3. Cost Optimization:
#    - Enable both caches (HTTP and LLM) for development
#    - Use lower MAX_PAGES_PER_SITE to limit token usage
#    - Monitor usage at: https://console.anthropic.com/
#
# 4. Troubleshooting:
#    - Set LOG_LEVEL=DEBUG for detailed logs
#    - Disable caches to ensure fresh data: ENABLE_HTTP_CACHE=false
#    - Increase timeouts for slow documentation sites
#
